var message = "hello world";
//create finding element
var heading = document.createElement("h1");
heading.textContent = message;
//apeend child to dom
document.body.appendChild(heading);
function getProduct(id) {
    {
        return {
            id: id,
            name: "the text for id = ".concat(id),
            price: 99.56,
        };
    }
}
heading.requer();
